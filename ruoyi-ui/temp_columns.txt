            <el-table-column prop="supplier" label="供应商" width="100">
              <template slot-scope="scope">
                <el-tag size="mini" type="success">{{ scope.row.supplier }}</el-tag>
              </template>
            </el-table-column>

            <!-- 成交价 -->
            <el-table-column label="成交价" width="120">
              <template slot-scope="scope">
                <div class="price-cell">
                  <div class="price-item">{{ scope.row.prices.original }}</div>
                  <div class="price-item">{{ scope.row.prices.wholesale }}</div>
                  <div class="price-item">{{ scope.row.prices.discount }}</div>
                  <div class="price-item final-price">{{ scope.row.prices.final }}</div>
                </div>
              </template>
            </el-table-column>

            <!-- 账期 -->
            <el-table-column label="账期" width="80">
              <template slot-scope="scope">
                <el-tag size="mini" type="info">{{ scope.row.paymentTerm }}</el-tag>
              </template>
            </el-table-column>

            <!-- 质量要求 -->
            <el-table-column label="质量要求" width="160">
              <template slot-scope="scope">
                <div class="quality-cell">
                  <div class="quality-main">{{ scope.row.qualityRequirement.main }}</div>
                  <div class="quality-detail">{{ scope.row.qualityRequirement.detail }}</div>
                </div>
              </template>
            </el-table-column>

            <!-- 串码信息 -->
            <el-table-column label="串码信息" width="140">
              <template slot-scope="scope">
                <div class="serial-cell">
                  <span class="serial-code">{{ scope.row.serialCode }}</span>
                  <i class="el-icon-copy-document copy-icon" @click="copyOrderNumber(scope.row.serialCode)"></i>
                </div>
              </template>
            </el-table-column>

            <!-- 物流信息 -->
            <el-table-column label="物流信息" width="140">
              <template slot-scope="scope">
                <div class="logistics-cell">
                  <span class="logistics-code">{{ scope.row.logisticsInfo }}</span>
                  <i class="el-icon-copy-document copy-icon" @click="copyOrderNumber(scope.row.logisticsInfo)"></i>
                </div>
              </template>
            </el-table-column>

            <el-table-column label="操作" width="160" fixed="right">
