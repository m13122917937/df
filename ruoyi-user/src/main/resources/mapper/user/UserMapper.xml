<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.user.mapper.UserMapper">

    <resultMap type="User" id="UserResult">
        <result property="userId" column="user_id"/>
        <result property="unionId" column="union_id"/>
        <result property="openId" column="open_id"/>
        <result property="phone" column="phone"/>
        <result property="nickName" column="nick_name"/>
        <result property="createTime" column="create_time"/>
        <result property="disable" column="disable"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <select id="companyUser" resultType="com.ruoyi.user.domain.dto.CompanyUserDTO">
        select
            u.*,
            uc.owner
        from u_user u
        join u_user_company uc on u.user_id = uc.user_id
            where
        uc.company_id = #{companyId}
        <if test="mobile!=null and mobile!=''">
            and u.phone like concat('%',#{mobile},'%')
        </if>
        <if test="nickName!=null and nickName!=''">
            and u.nick_name like concat('%',#{nickName},'%')
        </if>
    </select>



</mapper>