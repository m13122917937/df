<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.user.mapper.MemberWechatIdentityConflictMapper">
    <select id="selectOpenConflict" resultType="com.ruoyi.user.domain.MemberWechatIdentityConflict">
        SELECT * FROM u_member_wechat_identity_conflict
        WHERE union_id = #{unionId} AND source_member_id = #{sourceMemberId} AND target_member_id = #{targetMemberId}
          AND conflict_status = 'CONFLICT'
        ORDER BY id DESC LIMIT 1
    </select>
    <select id="selectListByStatus" resultType="com.ruoyi.user.domain.MemberWechatIdentityConflict">
        SELECT * FROM u_member_wechat_identity_conflict
        <where><if test="status != null and status != ''">AND conflict_status = #{status}</if></where>
        ORDER BY create_time DESC
    </select>
</mapper>
