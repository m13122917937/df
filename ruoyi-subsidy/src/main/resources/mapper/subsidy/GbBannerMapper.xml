<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.subsidy.mapper.GbBannerMapper">
    <select id="selectEnabled" resultType="com.ruoyi.subsidy.domain.GbBanner">
        SELECT * FROM gb_banner WHERE status = 1 ORDER BY sort_order ASC, id DESC
    </select>
</mapper>
