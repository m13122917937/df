<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.subsidy.mapper.GbPaymentMapper">
    <select id="selectByOrderId" resultType="com.ruoyi.subsidy.domain.GbPayment">
        SELECT * FROM gb_payment WHERE order_id = #{orderId} LIMIT 1
    </select>
    <select id="selectByPaymentNo" resultType="com.ruoyi.subsidy.domain.GbPayment">
        SELECT * FROM gb_payment WHERE payment_no = #{paymentNo} LIMIT 1
    </select>
    <update id="markPaidIfPending">
        UPDATE gb_payment
        SET payment_status = 'PAID', wechat_transaction_id = #{transactionId}, paid_time = NOW()
        WHERE id = #{paymentId} AND payment_status = 'PENDING'
    </update>
</mapper>
