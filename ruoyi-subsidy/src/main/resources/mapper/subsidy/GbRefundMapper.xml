<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.subsidy.mapper.GbRefundMapper">
    <select id="selectByRefundNo" resultType="com.ruoyi.subsidy.domain.GbRefund">
        SELECT * FROM gb_refund WHERE refund_no = #{refundNo} LIMIT 1
    </select>
    <update id="updateStatus">
        UPDATE gb_refund SET refund_status = #{target}, wechat_refund_id = COALESCE(#{wechatRefundId}, wechat_refund_id),
        refund_time = CASE WHEN #{target} = 'REFUNDED' THEN NOW() ELSE refund_time END
        WHERE id = #{refundId} AND refund_status = #{expected}
    </update>
    <select id="selectAdminList" resultType="com.ruoyi.subsidy.domain.GbRefund">
        SELECT * FROM gb_refund
        <where><if test="refundStatus != null and refundStatus != ''">AND refund_status = #{refundStatus}</if></where>
        ORDER BY create_time DESC
    </select>
</mapper>
