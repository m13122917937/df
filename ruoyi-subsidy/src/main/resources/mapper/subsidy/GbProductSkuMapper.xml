<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.subsidy.mapper.GbProductSkuMapper">
    <update id="adjustStock">
        UPDATE gb_product_sku
        SET stock_quantity = stock_quantity + #{delta}, update_time = NOW()
        WHERE id = #{skuId}
    </update>
</mapper>
